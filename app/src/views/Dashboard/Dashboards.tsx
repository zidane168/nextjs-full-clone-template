"use client"

import { Box } from "@mui/material"

const dashboardContainer = {
    height: "fit-content",
    width: "100%",
    padding: "24px",
}

const Dashboard = () => {
    // const dispatch = useAppDispatch()
    // const t = useTranslations()
    // const data = yup.object().shape({
    // date: yup.date(),
    // })
    // const { control, getValues } = useForm({ resolver: yupResolver<any>(data) })

    return <Box sx={dashboardContainer}></Box>
}

export default Dashboard
