import { SliceCaseReducers, ValidateSliceCaseReducers } from "@reduxjs/toolkit";
import { IUserState } from "@/types/user";

export const reducerActions: ValidateSliceCaseReducers<IUserState, SliceCaseReducers<IUserState>> = {
}
