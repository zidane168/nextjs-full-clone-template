import { SliceCaseReducers, ValidateSliceCaseReducers } from "@reduxjs/toolkit";
import { IBannerState } from "@/types/banner";

export const reducerActions: ValidateSliceCaseReducers<IBannerState, SliceCaseReducers<IBannerState>> = {}
