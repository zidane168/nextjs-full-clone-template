import { IDistrictState } from "@/types/district";
import { SliceCaseReducers, ValidateSliceCaseReducers } from "@reduxjs/toolkit";

export const reducerActions: ValidateSliceCaseReducers<IDistrictState, SliceCaseReducers<IDistrictState>> = {
}
