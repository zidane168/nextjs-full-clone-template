import { ISettingState } from "@/types/setting";
import { SliceCaseReducers, ValidateSliceCaseReducers } from "@reduxjs/toolkit";

export const reducerActions: ValidateSliceCaseReducers<ISettingState, SliceCaseReducers<ISettingState>> = {
}
