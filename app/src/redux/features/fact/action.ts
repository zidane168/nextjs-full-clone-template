import { IFactState } from "@/types/fact";
import { SliceCaseReducers, ValidateSliceCaseReducers } from "@reduxjs/toolkit";

export const reducerActions: ValidateSliceCaseReducers<IFactState, SliceCaseReducers<IFactState>> = {}
